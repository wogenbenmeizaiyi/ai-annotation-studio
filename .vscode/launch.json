{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "AI Studio: 完整启动（外部环境）",
      "type": "node-terminal",
      "request": "launch",
      "command": "powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File \"${workspaceFolder}\\scripts\\dev.ps1\" -Profile full -Infra external",
      "cwd": "${workspaceFolder}"
    }
  ]
}
